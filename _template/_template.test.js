const day = require("./#DAYSTRING.js");

describe("#DAYSTRING", () => {
  describe("part one", () => {
    test("works with demo", () => {});
  });

  describe("part two", () => {
    test("works with demo", () => {});
  });
});
