const day = require("./day-.js");

describe("day-", () => {
  describe("part one", () => {
    test("returns something", () => {
      expect(day.part1()).toBe(1);
    });
  });

  describe("part two", () => {
    test("returns something", () => {
      expect(day.part2()).toBe(2);
    });
  });
});
